{
  "name": "ChatCore",
  "packageManager": "yarn@4.10.3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "server": "yarn workspace server start:dev",
    "web": "yarn workspace web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
